namespace RuntimeNodeEditor
{
    public enum MathOperations
    {
        Multiply,
        Divide,
        Add,
        Subtract
    }

    public enum ConnectionType
    {
        Single,
        Multiple
    }
}
